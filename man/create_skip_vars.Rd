% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_skips.R
\name{create_skip_vars}
\alias{create_skip_vars}
\title{Flags de saltos inválidos por variable (presencia cuando NO es relevante)}
\usage{
create_skip_vars(data, spec, prefix = "s", numeric_conds = TRUE)
}
\arguments{
\item{data}{data.frame/tibble con los datos.}

\item{spec}{tibble con al menos \code{var}, y opcionalmente:
\itemize{
\item \code{when_vars} (list-col chr) y \code{when_values} (list-col) para condiciones.
\item \code{how} ("all"/"any") o \code{manual_expr} (expresión con AND/OR/NOT o &/|/!).
}}

\item{prefix}{Prefijo de las columnas de salto. Default \code{"s"}.}

\item{numeric_conds}{Si TRUE (default), compara \code{when_vars} numéricamente y
exige \code{when_values} numéricos.}
}
\value{
\code{data} con columnas nuevas \verb{s_<var>}, más \code{total_skips} y \code{flag_skips}.
}
\description{
Marca 1 cuando la variable tiene dato (!is.na()) y su condición de relevancia
evalúa a FALSE. Si la relevancia es NA (indeterminada), NO se flaggea.
Para reglas sin condiciones (obligatorias para todos), no aplica salto y
siempre devuelve 0.
}
