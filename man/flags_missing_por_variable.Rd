% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_missings.R
\name{flags_missing_por_variable}
\alias{flags_missing_por_variable}
\title{Flags de missing por variable con relevancia específica por variable}
\usage{
flags_missing_por_variable(
  data,
  spec,
  prefix = "m",
  na_when_not_relevant = FALSE,
  numeric_conds = TRUE,
  coerce_target = FALSE
)
}
\arguments{
\item{data}{data.frame o tibble con los datos.}

\item{spec}{tibble/data.frame con al menos la columna \code{var}. Opcionalmente:
\itemize{
\item \code{when_vars} (list-col chr) y \code{when_values} (list-col) para condiciones.
\item \code{how} ("all"/"any") o \code{manual_expr} (expresión AND/OR/NOT).
}}

\item{prefix}{Prefijo para los flags. Default "m".}

\item{na_when_not_relevant}{Si TRUE, pone NA cuando no es relevante (cuando SÍ hay condiciones).
Ignorado si no hay condiciones (relevancia incondicional).}

\item{numeric_conds}{Si TRUE, \code{when_vars} se comparan como numéricas y \code{when_values} deben ser numéricas.}

\item{coerce_target}{Si TRUE, fuerza a numérica la variable objetivo antes de \code{is.na()}.}
}
\value{
\code{data} con columnas nuevas \verb{<prefix>_<var>}.
}
\description{
Si una regla del \code{spec} NO tiene condiciones (sin \code{when_vars} o vacías),
se considera "obligatoria para todos" y el flag se define únicamente como
\code{is.na(var)} (1 si está missing, 0 si no), sin aplicar relevancia.
}
